I)-->
    int n = str.length()-1;
    for(int i=0,j=n; i<j; i++,j--)
    {
        int temp = str[i];
        str[i] = str[j];
        str[j] = temp;
    }
    return str;

II)-->
String rev = "";
       for(int i = str.length()-1;i>=0;i--){
        rev = rev + str.charAt(i);
       }
       return rev;
       
