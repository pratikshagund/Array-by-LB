int maxProfit(int arr[], int n)
{
    int maxm = 0;
    for(int i=1; i<n; i++)
    {
        if(arr[i]>arr[i-1])
        {
            maxm += arr[i]-arr[i-1];
        }
    }
    return maxm;
}

**********************************
o(n)
o(1)
*********************************
